services:
  - type: web
    name: ai_vending_machine
    env: python
    plan: free
    # buildCommand: |
    #   apt-get update && apt-get install -y ffmpeg
    #   pip install -r requirements.txt
    startCommand: uvicorn filehandle_databasess_copy:app --host 0.0.0.0 --port 8000
    envVars:
      - key: OPENAI_API_KEY
        sync: false
